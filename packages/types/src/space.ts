export type spaceMeta = {
  name: string;
  width: number;
  height: number;
  backgroundImagePath?: string;
  currentUserRole: string;
};

export type speceElementMeta = {
  spaceId: string;
  elementId: string;
  width: number;
  height: number;
};
